class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int m=0,temp=INT_MIN;
        for(int i=0;i<nums.size();i++)
        {
            m+=nums[i];
            if(m>temp)
                temp=m;
            if(m<0)
                m=0;
        }
        return temp;
    }
};